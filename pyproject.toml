[project]
name = "gitlab-bot-mr-review"
version = "0.1.0"
description = "GitLab webhook bot that automatically reviews merge requests using Claude AI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.43.0",
    "python-gitlab>=5.0.0",
    "pyyaml>=6.0.0",
]
